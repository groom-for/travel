import "../../styles/checkout.css";

function CheckoutLayout({ children }) {
  return (
    <main className="checkout-layout checkout-page">
      {children}
    </main>
  );
}

export default CheckoutLayout;
